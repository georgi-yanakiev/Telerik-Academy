// Declare an integer variable and assign it with
// the value 254 in hexadecimal format. Use Windows
// Calculator to find its hexadecimal representation.

using System;

class HexadecimalDeclaration
{
    static void Main(string[] args)
    {
        Console.Title = "Hexadecimal Declaration";
        int hex = 0xFE;
        Console.WriteLine(hex);
    }
}
